{
  "name": "outset-spike",
  "version": "0.0.1",
  "private": true,
  "description": "Week-1 spike for Outset: validate Claude Agent SDK + subscription auth + child-process invocation.",
  "type": "module",
  "scripts": {
    "spike": "tsx src/spike.ts",
    "spike:child": "tsx src/parent.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.119"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
